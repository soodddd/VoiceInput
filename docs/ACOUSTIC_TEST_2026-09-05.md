# 2026-09-05 实际声学链路测试（进行中）

目标：播放固定中英文语音，经外部音响和真实麦克风录制，验证识别、目标窗口输入、延迟、重复使用和异常恢复。

## 基线发现

- 系统列出 COLORFIRE CL500 音频输出端点、Realtek Speaker 和英特尔阵列麦克风；实际默认输出路由尚待核实。
- 已使用本机 System.Speech 生成固定中英文 WAV，脚本为 `scripts/make_speech_fixtures.ps1`，输出在忽略提交的 `test-artifacts/speech/`。
- 08:33:46 启动桌面程序，08:34:15 后端就绪，冷启动约 29 秒。
- 08:35:04 开始加载本地模型，08:35:07 加载完成（日志 2.7 秒）。
- 中文 WAV 播放测试失败：08:35:40.722 初始化录音，08:35:42.035 开始采集，08:35:42.739 就触发“两秒静音”，仅保存 33600 个采样（48000 Hz、0.7 秒）。界面返回 No speech detected。
- 原因：VAD 时间戳在 WASAPI 初始化前创建，初始化耗时计入静音时长。已改为 stream.play 成功后、接收采样前重置时间戳。Rust 16 项现有测试通过；尚需修复后二次声学测试。
- 发布一致性问题：当前桌面 EXE 为 0.1.3，实际 sidecar 启动日志仍为 0.1.2。下一次交付必须重建后端，并核实包内运行版本。
- 加载模型时界面短暂退回后端启动页。需要检查健康端点是否被模型锁阻塞。
- 独立硬件探针已确认默认输出为“耳机 (COLORFIRE CL500)”，默认输入为“麦克风阵列…”；播放时麦克风 RMS 为 -20.5 dB、峰值 -0.1 dB，不播放时 RMS 为 -104.2 dB。蓝牙音箱、系统路由和麦克风硬件正常。
- 新重建的 sidecar 启动日志已显示 `v0.1.3`；此前的 0.1.2 记录只属于旧运行实例。

## 未完成验收

修复后 VoiceInput 内部中文/英文播放识别、目标文本框输入、快捷键、长句与停顿、重复录音、识别延迟、模型释放再加载、进程恢复，以及完整重建包的复验均未完成。独立硬件链路已证明正常；现有结果仍不证明成品已稳定可用。
